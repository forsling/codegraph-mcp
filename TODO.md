# TODO

## Dogfooding milestone

Goal: make `codegraph-mcp` useful enough that an agent can develop this repository primarily through its own MCP graph interface, while measuring source-vs-graph retrieval cost.

### P0 — unblock self-use

- [x] Add explicit symbol match semantics (`exact` vs `contains`) to `search_symbols`.
- [x] Fix the MCP smoke test to use exact resolution where appropriate.
- [x] Get CI green end-to-end: build, fixture index, MCP stdio calls, path traversal, source retrieval.
- [ ] Check in `package-lock.json` once generated by CI/local npm so installs are reproducible.
- [x] Add graph correctness assertions for calls, reads, writes, ambiguous names, and bounded paths.

### P1 — self-indexing

- [x] Index the `codegraph-mcp` repository itself in CI.
- [x] Add a self-dogfood MCP test answering structural questions about this codebase.
- [x] Verify the graph can locate `GraphStore.references`, `GraphStore.findPaths`, MCP handlers, and call paths into SQLite-backed store methods.
- [ ] Add stale-index detection before relying on graph facts after edits.

### P2 — measure retrieval economics

- [x] Instrument dogfood MCP responses with response bytes and estimated token counts.
- [x] Record graph/navigation tokens separately from source tokens.
- [x] Add per-run summaries by tool plus graph/source/total retrieval.
- [ ] Track symbols opened and files touched explicitly.
- [ ] Define and capture `source tokens before first correct edit` in the agent-task harness.

### P3 — first agent A/B dogfood experiment

- [ ] Pick one real repository task after the current exact-match change, so both conditions work from the same baseline.
- [ ] Run condition A with normal filesystem/search/source tools.
- [ ] Run condition B with codegraph MCP as the primary navigation tool and source-by-symbol retrieval.
- [ ] Compare task success, source tokens, total retrieval tokens, tool calls, and time-to-relevant-symbol.
- [ ] Review the MCP trajectory for missing query primitives before adding new features.

### P4 — only after dogfood evidence

- [ ] Add new graph capabilities only when dogfooding exposes a concrete navigation gap.
- [ ] Consider bounded dataflow before embeddings or UI.
- [ ] Defer runtime traces, Python support, generalized query language, semantic summaries, and visual IDE work until the core retrieval hypothesis is validated.

## Working principle

New graph/query features should preferably be motivated by a real question that was awkward or expensive to answer while dogfooding the existing system.
